{
  "name": "partydecor-api",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@aurea/web",
    "build": "npm run build --workspace=@aurea/web",
    "start": "npm run start --workspace=@aurea/web"
  },
  "overrides": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
